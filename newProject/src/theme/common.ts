import { ImageStyle, TextStyle, View, ViewStyle } from 'react-native';

export const bgStyle: ViewStyle = {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%'
}

export const fullStyle: ViewStyle = {
    width: '100%',
    height: '100%'
}

export const imageFullStyle: ImageStyle = {
    width: '100%',
    height: '100%'
}

export const circleStyle: ViewStyle = {
    borderRadius: 500
}